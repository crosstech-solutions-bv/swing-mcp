package solutions.crosstech.swingmcp.common.enums;

/** Condition type for wait_for tool. */
public enum WaitConditionType {
    WINDOW_TITLE,
    COMPONENT_TEXT,
    COMPONENT_VISIBLE,
    COMPONENT_ENABLED,
    COMPONENT_EXISTS,
    COMPONENT_GONE,
    WINDOW_COUNT,
    EDT_IDLE
}
